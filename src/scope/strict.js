'use strict';
pi = 3.1416;
console.log(pi);

function myFunction() {
	'use strict'
	return pi = 3.1416;
}

console.log(myFunction());

// Cuando usamos el modo estricto en JavaScrip. Siempre hay que declarar las variables antes de inicializarlas. 